{
  "name": "daily-habit",
  "version": "3.0.1",
  "description": "Daily Habit — streak tracker with optional accounts and cross-device sync (SQLite or Postgres)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "pg": "^8.23.0"
  }
}
