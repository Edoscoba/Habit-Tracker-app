{
  "name": "daily-habit",
  "version": "3.1.0",
  "description": "Daily Habit — streak tracker with accounts, cross-device sync and push reminders (SQLite or Postgres)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "pg": "^8.23.0",
    "web-push": "^3.6.7"
  }
}
